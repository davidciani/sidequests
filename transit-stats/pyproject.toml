[project]
name = "transit-stats"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "David Ciani", email = "dciani@davidciani.com" }
]
requires-python = ">=3.14"
dependencies = [
    "polars>=1.44.1",
]

[project.scripts]
transit-stats = "transit_stats:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
